# Intentional exceptions for `docs_link_guard::every_cited_docs_path_resolves`.
#
# Format: `docs/path.md — reason`  (em dash). A trailing `*` is a prefix glob.
# Blank lines and `#` comments ignored.
#
# Prefer rewording over adding an entry: a path in backticks is a promise that
# it resolves. Legitimate uses are (a) a plan naming a tree it proposes to
# create, and (b) a path that must stay literal for another reason.

# --- docs/design/docs-reorganization-plan.md proposes this tree (PR 2 creates
# --- it). Delete these six globs when the move lands; until then nothing can
# --- exist under them, so they mask nothing.
docs/start/* — proposed tree, docs-reorganization-plan.md §3
docs/guide/* — proposed tree, docs-reorganization-plan.md §3
docs/reference/* — proposed tree, docs-reorganization-plan.md §3
docs/concepts/* — proposed tree, docs-reorganization-plan.md §3
docs/internals/* — proposed tree, docs-reorganization-plan.md §3
docs/proposals/* — proposed tree, docs-reorganization-plan.md §3
docs/reports/* — proposed tree, docs-reorganization-plan.md §3
